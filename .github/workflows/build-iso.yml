name: Build LegacyOS ISO

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install build dependencies & Debian Keyrings
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            live-build \
            debootstrap \
            debian-archive-keyring \
            debian-keyring \
            squashfs-tools \
            xorriso \
            grub-pc-bin \
            grub-efi-amd64-bin \
            mtools \
            dosfstools

      - name: Fix debootstrap Trixie support on Ubuntu host
        run: |
          # Ubuntu's debootstrap sometimes lacks the trixie script; link it to sid
          if [ ! -e /usr/share/debootstrap/scripts/trixie ]; then
            sudo ln -s sid /usr/share/debootstrap/scripts/trixie
          fi

      - name: Configure Live-Build
        run: |
          lb config \
            --distribution trixie \
            --architectures amd64 \
            --binary-images iso-hybrid \
            --archive-areas "main contrib non-free non-free-firmware" \
            --mirror-bootstrap "http://deb.debian.org/debian/" \
            --mirror-chroot "http://deb.debian.org/debian/" \
            --debootstrap-options "--keyring=/usr/share/keyrings/debian-archive-keyring.gpg" \
            --apt-indices false \
            --memtest none

      - name: Build ISO
        run: |
          sudo lb build

      - name: Verify & Rename ISO
        run: |
          if [ -f live-image-amd64.hybrid.iso ]; then
            mv live-image-amd64.hybrid.iso LegacyOS-1.0-amd64.iso
          else
            echo "Error: ISO was not created by live-build."
            exit 1
          fi

      - name: Upload ISO Artifact
        uses: actions/upload-artifact@v4
        with:
          name: LegacyOS-1.0-ISO
          path: LegacyOS-1.0-amd64.iso
          if-no-files-found: error
